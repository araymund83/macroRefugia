#--------------------------------------------
## Set paths
#--------------------------------------------
paths <- list(
  cachePath = checkPath("cache", create = TRUE),
  inputPath = file.path("inputs"),
  outputPath = file.path("outputs")
)

